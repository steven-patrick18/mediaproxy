# HOMER stack secrets. Real .env file lives at /opt/homer/.env on the
# base-app and is git-ignored.
#
# Generate with:
#   openssl rand -base64 24 | tr -d '=+/' | cut -c1-32
HOMER_DB_PASSWORD=replace-me-with-a-strong-password
